module pdftoolkit

// Polyglot PDF & Image toolkit — Go mirror of the shared ubiquitous language.
// See ../DOMAIN.md. No external deps yet: the Ghostscript engine uses os/exec
// (stdlib); pdfcpu + golang.org/x/image are the planned adapters (stubbed).
go 1.23
