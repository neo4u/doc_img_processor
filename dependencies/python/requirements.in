pikepdf>=9.3.0       # MPL-2.0 — primary PDF surgery engine
Pillow>=11.0.0       # HPND — image codec
numpy>=2.0.0         # BSD — SSIM meter
pypdfium2>=4.30.0    # BSD/Apache — zero-AGPL PDF renderer
PyMuPDF>=1.25.0      # AGPL — optional inspector; confined to pymupdf_codec.py
pillow-heif>=0.18.0  # BSD-3 (bundles libheif LGPL) — HEIC decode; confined to heif adapter
mozjpeg-lossless-optimization>=1.1.0  # MIT — truly lossless JPEG optimize (Huffman)
fastapi>=0.115.0     # MIT — HTTP API layer
uvicorn>=0.32.0      # BSD — ASGI server
python-multipart>=0.0.12  # Apache — multipart upload parsing for FastAPI
mcp>=1.2.0           # MIT — Model Context Protocol server for Claude Code / cowork
